---
deployment:
  tasks:
    - export DEPLOYPATH=/home/c2268453c/public_html/strax/
    - /bin/mkdir -p $DEPLOYPATH
    - /bin/cp -R app config database public scripts storage $DEPLOYPATH
    - /bin/cp index.php install.sql composer.json composer.lock .htaccess manifest.webmanifest $DEPLOYPATH
    - /bin/cp tiktokEBsMEMZkBn4noYozg2YzMpwlpf1Zb58I.txt $DEPLOYPATH
    - if [ -x /opt/cpanel/composer/bin/composer ]; then /opt/cpanel/composer/bin/composer install --no-dev --prefer-dist --optimize-autoloader --working-dir=$DEPLOYPATH; elif command -v composer >/dev/null 2>&1; then composer install --no-dev --prefer-dist --optimize-autoloader --working-dir=$DEPLOYPATH; fi
    - test -f $DEPLOYPATH/app/third_party/tcpdf/tcpdf.php
    - test -f $DEPLOYPATH/app/third_party/tcpdf/fonts/dejavusans.php
